模型
    1.一个生产者多个消费者
    2.每一个消费者都有自己的队列
    3.生产者没有直接把信息发送到队列上，而是发送到交换机exchange
    4.每个队列都要绑定到交换机上
    5.生产者发送的消息经过交换机到达队列，就可以实现一个消息被多个消费者消费。

交换机
    交换机没有存储能力，只有队列有存储能力。如果没有队列绑定交换机，数据就会丢失。

    匿名转发：交换机名称为""

    交换机类型
        fanout:不处理路由键
            每个人都会收到
        direct:处理路右键
            根据绑定key进行选择分发
